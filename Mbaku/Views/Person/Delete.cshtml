@model TblPerson
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>Are you sure you want to delete this record ?</h4>

<dl>
    <dt>@Html.DisplayNameFor(c => c.Id)</dt>
    <dd> @Html.DisplayFor(c => c.Id) </dd>    
</dl>
<dl>
    <dt>@Html.DisplayNameFor(c => c.Name)</dt>
    <dd> @Html.DisplayFor(c => c.Name) </dd>
</dl>
<dl>
    <dt>@Html.DisplayNameFor(c => c.Email)</dt>
    <dd> @Html.DisplayFor(c => c.Email) </dd>
</dl>

<form asp-action="Delete" asp-controller="Person">
    <input type="submit" value="Delete" class="btn-danger" />
    <a asp-action="Index" asp-controller="Person" class="btn-primary">Return</a>
</form>
